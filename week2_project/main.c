#include <stdio.h>
#include <stdlib.h>

int main()
{
    int getal1;
    int getal2;
    int getal3;

    printf("Voer drie getallen in. \n");
    scanf("%d", &1);
    scanf("%d", &2);
    scanf("%d", &3);

    if(getal1 > getal2);
    {
        if getal(getal1 > getal3);
        {
            grootste = getal1;
        }
        else
        {
            grootste = getal3;
        }
    }
    else
    {
            {
        if getal(getal2 > getal3);
        {
            grootste = getal2);
        }
        else
        {
            grootste = getal3;
        }
    }
    }
    printf(grootste is %d\n, grootste);
    return 0;
}
